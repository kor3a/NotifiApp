// Google Maps / Places API key used by the JS layer (Places web service for
// locating store branches on the Map tab). This is the same key wired into the
// native build via android/gradle.properties (MAPS_API_KEY).
//
// For the Map tab to show store pins, this key must have BOTH "Maps SDK for
// Android" and "Places API" enabled in Google Cloud (project georeminder-pilot).
export const GOOGLE_MAPS_API_KEY = 'AIzaSyDfqvH3s2BwiwmAy46AT4NLYAjboTF4gkY';
