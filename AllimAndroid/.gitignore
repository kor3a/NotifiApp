# Node
node_modules/
npm-debug.log
yarn-error.log
package-lock.json

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native projects are generated by `npx expo prebuild` (CNG) — never edit or
# commit them. Everything that shapes them lives in app.config.js and plugins/.
/android
/ios

# Misc
.DS_Store
*.log
.env
.env.local
